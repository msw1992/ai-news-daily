# 生产环境部署指南

本指南将帮助你把每日AI资讯网站部署到生产环境，让所有人都能通过公网链接访问。

## 架构说明

```
┌─────────────────┐     ┌─────────────────┐
│   前端 (Vercel) │────▶│  后端 (Render)  │
│  静态网站托管    │     │  Node.js 服务   │
└─────────────────┘     └─────────────────┘
        │                       │
        ▼                       ▼
   用户访问              爬取RSS/API
   https://xxx          https://xxx/api
```

## 部署方案对比

| 方案 | 前端 | 后端 | 费用 | 推荐度 |
|-----|------|------|------|-------|
| 方案A | Vercel | Render | 免费 | ⭐⭐⭐⭐⭐ |
| 方案B | Vercel | Railway | 免费(有限) | ⭐⭐⭐⭐ |
| 方案C | Netlify | Render | 免费 | ⭐⭐⭐⭐ |

**推荐方案A**：Vercel + Render，完全免费且稳定。

---

## 第一步：准备代码仓库

### 1.1 初始化Git仓库

```bash
cd ai-news-daily

# 初始化git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: AI News Daily"
```

### 1.2 推送到GitHub

1. 访问 https://github.com/new 创建新仓库
2. 仓库名建议：`ai-news-daily`
3. 不要勾选 README、.gitignore（已有）
4. 创建后，按提示推送代码：

```bash
git remote add origin https://github.com/你的用户名/ai-news-daily.git
git branch -M main
git push -u origin main
```

---

## 第二步：部署后端 (Render)

### 2.1 注册Render

1. 访问 https://dashboard.render.com/
2. 点击 "Get Started" 使用GitHub账号登录
3. 授权Render访问你的GitHub仓库

### 2.2 创建Web Service

1. 点击 "New +" → "Web Service"
2. 选择你刚创建的 `ai-news-daily` 仓库
3. 配置如下：

| 配置项 | 值 |
|-------|-----|
| Name | `ai-news-server` |
| Region | `Oregon (US West)` 或 `Singapore` |
| Branch | `main` |
| Root Directory | `server` |
| Runtime | `Node` |
| Build Command | `npm install` |
| Start Command | `node server.js` |
| Plan | `Free` |

4. 点击 "Advanced" 添加环境变量：

| Key | Value |
|-----|-------|
| NODE_ENV | production |

5. 点击 "Deploy Web Service"

### 2.3 获取后端URL

部署完成后，你会获得一个URL，类似：
```
https://ai-news-server.onrender.com
```

**记下这个URL，后面需要用到！**

### 2.4 验证后端

访问以下URL验证部署成功：
```
https://你的后端URL/api/health
```

应该返回：
```json
{"status":"ok","mode":"real-crawler","timestamp":"..."}
```

---

## 第三步：部署前端 (Vercel)

### 3.1 注册Vercel

1. 访问 https://vercel.com/
2. 点击 "Sign Up" 使用GitHub账号登录
3. 授权Vercel访问你的GitHub仓库

### 3.2 导入项目

1. 点击 "Add New..." → "Project"
2. 选择 `ai-news-daily` 仓库
3. 配置如下：

| 配置项 | 值 |
|-------|-----|
| Framework Preset | `Vite` |
| Root Directory | `./` (保持默认) |
| Build Command | `npm run build` |
| Output Directory | `dist` |

4. **重要**：点击 "Environment Variables" 添加：

| Name | Value |
|------|-------|
| VITE_API_URL | `https://你的后端URL` |

例如：
```
VITE_API_URL=https://ai-news-server.onrender.com
```

5. 点击 "Deploy"

### 3.3 等待部署

部署大约需要1-2分钟，完成后你会看到庆祝动画！

### 3.4 获取前端URL

部署成功后，你会获得一个URL：
```
https://ai-news-daily.vercel.app
```

或者类似 `https://ai-news-daily-xxx.vercel.app`

---

## 第四步：验证部署

### 4.1 访问网站

打开你的前端URL，应该能看到：
- 网站正常加载
- 显示AI资讯列表
- 可以切换分类、搜索、收藏

### 4.2 检查数据

如果显示"获取资讯失败"，检查：
1. 后端是否正常运行（访问 `/api/health`）
2. 前端环境变量 `VITE_API_URL` 是否正确设置

---

## 第五步：自定义域名（可选）

### 5.1 在Vercel添加域名

1. 进入项目 → Settings → Domains
2. 输入你的域名，如 `ai-news.yourdomain.com`
3. 按提示在域名服务商处添加DNS记录

### 5.2 在Render添加域名

1. 进入项目 → Settings → Custom Domains
2. 添加你的API域名，如 `api.yourdomain.com`
3. 更新前端的 `VITE_API_URL` 环境变量

---

## 常见问题

### Q: 后端启动失败？

检查Render日志，常见原因：
- `package.json` 依赖问题
- 端口配置错误（Render自动分配PORT环境变量）

### Q: 前端无法获取数据？

1. 检查浏览器控制台是否有CORS错误
2. 确认 `VITE_API_URL` 设置正确
3. 确认后端 `/api/health` 可以访问

### Q: 数据不更新？

后端每30分钟自动刷新一次，也可以：
1. 访问 `https://后端URL/api/news/refresh` 手动刷新
2. 在Render中重启服务

### Q: Render免费版限制？

- 每月750小时免费
- 服务15分钟无请求会休眠
- 首次请求可能需要等待30秒唤醒

---

## 项目文件说明

部署相关文件：

```
ai-news-daily/
├── vercel.json          # Vercel配置
├── render.yaml          # Render配置(前端)
├── .env.production      # 生产环境变量
├── .gitignore           # Git忽略文件
└── server/
    ├── render.yaml      # Render配置(后端)
    ├── railway.json     # Railway配置(备选)
    └── .dockerignore    # Docker忽略文件
```

---

## 部署清单

- [ ] 代码推送到GitHub
- [ ] 后端部署到Render
- [ ] 获取后端URL
- [ ] 前端部署到Vercel（设置VITE_API_URL）
- [ ] 验证网站正常访问
- [ ] （可选）配置自定义域名

---

## 技术支持

如遇问题，请检查：
1. Render部署日志
2. Vercel部署日志
3. 浏览器控制台错误信息

祝部署顺利！🎉
